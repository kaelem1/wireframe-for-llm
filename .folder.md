# Folder: /Users/kaelem/dev/wireframe-for-llm

1. **地位**：线框原型工具仓库根目录

2. **逻辑**：当前目录已独立为 Git 仓库根；Vite 启动 `src/` 前端原型编辑器；`vercel.json` 固定 Vercel 以 Vite 构建并发布 `dist/`；`.ai/` 保存当前进展与长期决策；各子目录用 `.folder.md` 维护局部认知。

3. **约束**：仅保留当前任务需要的文件；结构或职责变化时同步目录文档。

## 成员清单

- `.ai/`：项目状态与长期决策
- `src/`：应用源码
- `public/`：静态图标资源
- `index.html`：浏览器入口
- `package.json`：依赖与脚本
- `vercel.json`：Vercel 构建与输出目录配置
- `readme.md`：项目概览

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
