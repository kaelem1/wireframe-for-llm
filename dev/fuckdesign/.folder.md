# Folder: /Users/kaelem/dev/fuckdesign

1. **地位**：桌面原型工具工程根目录

2. **逻辑**：`Package.swift` 定义构建；`Sources/FuckDesign` 提供 App、编辑器、预览、AI 与导出；`Tests` 校验模型序列化

3. **约束**：禁止创建白名单外文档；根目录变更后同步本文件与 `.ai/current.md`

## 成员清单

- `Package.swift`：SwiftPM 工程定义
- `Makefile`：构建、运行、安装、清理入口
- `readme.md`：项目启动说明
- `.ai/`：当前任务与长期决策
- `Resources/`：App bundle 元数据
- `Sources/`：应用源码
- `Tests/`：模型与导出测试

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
