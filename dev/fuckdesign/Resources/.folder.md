# Folder: /Users/kaelem/dev/fuckdesign/Resources

1. **地位**：App bundle 元数据目录

2. **逻辑**：`Info.plist` 供 `make install` 组装 `.app` 时使用

3. **约束**：仅保存应用打包所需资源

## 成员清单

- `Info.plist`：应用 bundle 元数据

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
