# Folder: /Users/kaelem/dev/fuckdesign/Sources/FuckDesign

1. **地位**：主应用目标

2. **逻辑**：App 层装配窗口；Model 保存项目状态；Canvas/Screens/Inspector 组成编辑器；Preview/Services 提供运行时与外部能力

3. **约束**：按职责分目录，不在根目录堆叠业务实现

## 成员清单

- `FuckDesignApp.swift`：App 入口
- `App/`：窗口与命令系统
- `Model/`：可序列化模型与编辑状态
- `Canvas/`：画布与图元渲染
- `Screens/`：页面列表
- `Inspector/`：属性编辑
- `Preview/`：预览运行时
- `Services/`：AI 与导出

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
