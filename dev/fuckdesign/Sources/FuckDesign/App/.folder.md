# Folder: /Users/kaelem/dev/fuckdesign/Sources/FuckDesign/App

1. **地位**：窗口、文档和命令装配层

2. **逻辑**：`PrototypeDocument` 负责 `.aipro` 读写；`WorkspaceRootView` 组装页面侧栏、画布、属性面板、AI 调用和导出入口；`WorkspaceCommands` 暴露快捷键与导出命令

3. **约束**：只处理装配，不承载业务规则

## 成员清单

- `PrototypeDocument.swift`：`.aipro` 文件文档
- `WorkspaceRootView.swift`：文档窗口三栏编辑器、AI 面板和导出入口
- `WorkspaceCommands.swift`：菜单、快捷键与导出命令

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
