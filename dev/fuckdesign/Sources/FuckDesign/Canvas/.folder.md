# Folder: /Users/kaelem/dev/fuckdesign/Sources/FuckDesign/Canvas

1. **地位**：编辑态无限画布与图元渲染目录

2. **逻辑**：`InfiniteCanvasView` 负责网格、缩放平移、`Space+拖拽` 平移、图元绘制、选择拖拽、智能参考线显示

3. **约束**：只承载编辑态画布，不写入文档装配和服务逻辑

## 成员清单

- `InfiniteCanvasView.swift`：无限画布、图元渲染、拖拽和缩放入口

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
