# Folder: /Users/kaelem/dev/fuckdesign/Sources/FuckDesign/Inspector

1. **地位**：属性编辑与交互配置目录

2. **逻辑**：`InspectorView` 通过 `ProjectStore` 更新项目、AI 配置、图元样式、视觉状态、交互配置和层级动作

3. **约束**：只做编辑器右侧属性面板，不直接承担预览运行时或导出逻辑

## 成员清单

- `InspectorView.swift`：属性面板与状态/交互编辑入口

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
