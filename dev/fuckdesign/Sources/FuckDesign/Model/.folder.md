# Folder: /Users/kaelem/dev/fuckdesign/Sources/FuckDesign/Model

1. **地位**：原型项目数据模型与编辑状态目录

2. **逻辑**：`PrototypeModels` 定义 `.aipro` 结构；`ProjectStore` 管理当前编辑会话、选择、插入、撤销、吸附

3. **约束**：模型保持 Codable；运行态临时状态不要写回持久模型

## 成员清单

- `PrototypeModels.swift`：项目、页面、图元、交互、AI 配置模型
- `ProjectStore.swift`：编辑器状态与变更入口

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
