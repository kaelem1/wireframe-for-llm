# Folder: /Users/kaelem/dev/fuckdesign/Sources/FuckDesign/Services

1. **地位**：AI 调用、文档导出与页面截图服务层

2. **逻辑**：`LLMClient` 产出 `AIPrototypeSpec` 并抽取有效 JSON；`ProjectExportService` 生成并写出 Markdown/JSON；`ScreenshotExportService` 基于项目模型离屏绘制含箭头的 PNG

3. **约束**：只处理外部 API 和导出边界，不承接编辑态业务状态

## 成员清单

- `LLMClient.swift`：OpenAI 兼容请求与 AI JSON 解析
- `ProjectExportService.swift`：结构化 Markdown/JSON 导出与写盘
- `ScreenshotExportService.swift`：页面 PNG 离屏导出与箭头绘制

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
