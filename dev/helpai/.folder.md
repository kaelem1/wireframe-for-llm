# Folder: /Users/kaelem/dev/helpai

1. **地位**：低保真原型设计工具的仓库根目录，承载前端 SPA、过程文档与构建配置。

2. **逻辑**：`src/` 内部以项目列表页和编辑器双视图运行；项目数据经 Zustand + localStorage 持久化；Konva 负责画板编辑；SiliconFlow 用于 AI 生成与对齐测试。

3. **约束**：只做当前任务直接需要的前端能力；改动目录职责、数据流或长期约束时，必须同步本文件与白名单文档。

## 成员清单

- `package.json`：依赖、脚本与构建入口。
- `src/`：前端 SPA 源码。
- `readme.md`：项目说明、安装与能力概览。
- `.ai/current.md`：当前任务状态记录。
- `.ai/decisions.md`：长期约束与取舍记录。

## 触发器

本目录增删文件，或仓库职责、数据流、技术边界变化时，立即重写此文档。
