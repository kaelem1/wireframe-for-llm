# Folder: /Users/kaelem/dev/helpai/src

1. **地位**：前端 SPA 源码入口，承载页面、状态、纯逻辑与样式。

2. **逻辑**：`App.tsx` 在项目列表页与编辑器间切换；`store` 持久化项目；`features` 组织 UI；`lib` 提供导出、AI、预览与评分纯函数。

3. **约束**：优先把业务规则放入 `lib` / `store`，UI 只做装配；改动子目录职责时同步更新对应 `.folder.md`。

## 成员清单

- `App.tsx`：应用根组件。
- `main.tsx`：React 挂载入口。
- `index.css`：Tailwind 与全局样式。
- `data/`：设备预设常量。
- `types/`：核心类型定义。
- `lib/`：纯函数与 AI/导出逻辑。
- `store/`：Zustand 状态管理。
- `features/`：项目页与编辑器 UI。

## 触发器

本目录增删文件，或页面装配、状态流、纯逻辑边界变化时，立即重写此文档。
