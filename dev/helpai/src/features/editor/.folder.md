# Folder: /Users/kaelem/dev/helpai/src/features/editor

1. **地位**：核心编辑器模块。

2. **逻辑**：`EditorPage.tsx` 组织三栏工作区与 AI 工具；`BoardCanvas.tsx` 负责 Konva 画布编辑与预览渲染；`PreviewOverlay.tsx` 负责全屏预览。

3. **约束**：编辑规则通过 `store` 与 `lib` 驱动；不要在此重复实现业务计算。

## 成员清单

- `EditorPage.tsx`：编辑器主界面、属性面板、快捷键、AI 弹窗。
- `BoardCanvas.tsx`：画板渲染、拖拽、缩放、右键菜单、内联文字编辑。
- `PreviewOverlay.tsx`：全屏预览与切页动画容器。

## 触发器

本目录增删文件，或编辑器主链路、画布职责、预览结构变化时，立即重写此文档。
