# Folder: /Users/kaelem/dev/helpai/src/lib

1. **地位**：业务纯函数层。

2. **逻辑**：`project.ts` 负责工厂与历史快照；`exporters.ts` 负责 AI 导出；`preview.ts` 负责预览交互执行；`alignment.ts` 负责对齐评分；`ai.ts` 负责 SiliconFlow 请求；`storage.ts` 负责本地存储与下载。

3. **约束**：优先保持纯函数；不要在这里写 React 组件。

## 成员清单

- `project.ts`：项目、画板、元素、交互工厂与快照工具。
- `exporters.ts`：导出 JSON 与 Markdown。
- `alignment.ts`：AI 解读对齐评分。
- `preview.ts`：预览导航与显隐/状态切换执行器。
- `ai.ts`：SiliconFlow OpenAI 兼容调用层。
- `storage.ts`：localStorage 与文件下载封装。
- `*.test.ts`：纯函数与状态行为测试。

## 触发器

本目录增删文件，或导出格式、AI 协议、预览执行规则变化时，立即重写此文档。
