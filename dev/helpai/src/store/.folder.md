# Folder: /Users/kaelem/dev/helpai/src/store

1. **地位**：全局状态层。

2. **逻辑**：Zustand store 管理项目列表、当前编辑上下文、元素操作、交互配置、撤销重做与持久化保存。

3. **约束**：状态变更必须通过 action；不要把 UI 细节塞进持久化结构。

## 成员清单

- `useAppStore.ts`：应用 store、action 与测试入口。
- `useAppStore.test.ts`：历史记录与撤销重做测试。

## 触发器

本目录增删文件，或持久化结构、历史机制、action 边界变化时，立即重写此文档。
