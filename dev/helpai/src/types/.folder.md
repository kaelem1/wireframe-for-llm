# Folder: /Users/kaelem/dev/helpai/src/types

1. **地位**：原型工具数据契约中心。

2. **逻辑**：定义项目、画板、元素、交互、导出、对齐报告与预览会话结构，供 `store`、`lib` 与 `features` 共用。

3. **约束**：保持可序列化；不要放运行时逻辑。

## 成员清单

- `prototype.ts`：全局共享类型定义。

## 触发器

本目录增删文件，或项目数据结构、导出格式、预览会话结构变化时，立即重写此文档。
