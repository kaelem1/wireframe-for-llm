# Folder: /Users/kaelem/dev/ihelp/1

1. **地位**：项目根目录，承载构建配置、全局说明与状态文档。

2. **逻辑**：Vite 启动 `src/main.tsx`，`src/App.tsx` 作为主工作区编排层，React 应用通过 Zustand store 驱动编辑、预览、导入导出与 AI 调用。

3. **约束**：禁止创建白名单外文档；根目录配置变更后同步更新本文件与 `.ai/current.md`。

## 成员清单

- `package.json`：依赖与脚本入口。
- `index.html`：Vite 页面宿主。
- `tsconfig*.json`：TypeScript 编译配置。
- `vite.config.ts`：Vite 构建配置。
- `.ai/`：当前任务与长期决策文档。
- `src/`：全部业务源代码。
- `readme.md`：项目简介。

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
