# Folder: /Users/kaelem/dev/ihelp/1/src

1. **地位**：前端业务代码根目录。

2. **逻辑**：`main.tsx` 挂载应用，`App.tsx` 组装工作区、面板、快捷键与预览层，`components/` 提供可复用原语，`stores/` 管理状态与持久化，`utils/` 提供共享逻辑，`types/` 定义数据结构。

3. **约束**：所有源代码只放在 `src/`；目录职责变化后同步更新本文件与对应子目录文档。

## 成员清单

- `main.tsx`：应用挂载入口。
- `App.tsx`：工作区编排、模态层、快捷键与预览接线。
- `index.css`：全局样式。
- `components/`：画板、组件库、工具栏、画板条与新建项目原语。
- `stores/`：状态管理、历史与持久化接线。
- `types/`：类型定义。
- `utils/`：共享工具、JSON 校验、几何与 AI 调用。

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
