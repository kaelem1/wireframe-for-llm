# Folder: /Users/kaelem/dev/ihelp/1/src/components

1. **地位**：编辑器界面组件目录。

2. **逻辑**：左侧组件库发起添加动作，中间画板区负责编辑与预览渲染，底部画板条管理画板顺序，顶部工具栏负责项目级操作；交互/图层/AI/测试模态层由 `App.tsx` 统一编排。

3. **约束**：组件只消费 store 和 utils，不自行维护第二份项目数据。

## 成员清单

- `BoardSurface.tsx`：画板与组件块基础渲染。
- `Toolbar.tsx`：顶部工具栏。
- `ComponentLibrary.tsx`：左侧组件库。
- `BoardStrip.tsx`：底部画板条。
- `CreateProjectDialog.tsx`：项目创建弹层。

## 触发器

组件职责、布局分工或交互链路变化时，立即重写此文档。
