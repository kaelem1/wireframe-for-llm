# Folder: /Users/kaelem/dev/ihelp/1/src/stores

1. **地位**：全局编辑状态目录。

2. **逻辑**：`editorStore.ts` 维护项目、选中态、预览态、历史栈、面板状态与 localStorage 持久化接线，对外提供唯一写入口。

3. **约束**：项目结构修改必须走 store action，避免组件直接拼接 JSON。

## 成员清单

- `editorStore.ts`：Zustand 编辑器状态与动作。

## 触发器

状态流、撤销重做策略或持久化边界变化时，立即重写此文档。
