# Folder: /Users/kaelem/dev/ihelp/1/src/types

1. **地位**：原型 JSON 与运行时状态的类型契约目录。

2. **逻辑**：组件、交互、画板、项目、AI 配置等类型从这里导出，供 store、utils 和组件复用。

3. **约束**：导出类型要与 JSON 结构一致；避免在组件内定义重复结构。

## 成员清单

- `schema.ts`：项目、组件、交互、AI 与测试状态类型。

## 触发器

数据结构、导出字段或约束变更时，立即重写此文档。
