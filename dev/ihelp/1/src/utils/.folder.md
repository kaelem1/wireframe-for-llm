# Folder: /Users/kaelem/dev/ihelp/1/src/utils

1. **地位**：共享元数据、几何、导入导出、持久化与 LLM 调用目录。

2. **逻辑**：组件元数据驱动默认布局；几何工具处理吸附与边界；项目工具负责 JSON 校验/导入导出；存储工具负责本地持久化；LLM 工具封装 OpenAI 兼容调用。

3. **约束**：仅保留跨组件复用的纯函数或系统边界逻辑，不在此目录堆 UI 状态。

## 成员清单

- `catalog.ts`：设备与组件元数据。
- `geometry.ts`：吸附、对齐与约束计算。
- `project.ts`：项目 JSON 校验与导入导出。
- `storage.ts`：localStorage 与文件读写。
- `llm.ts`：OpenAI 兼容 API 调用与提示词。

## 触发器

共享逻辑、系统边界或导出格式变化时，立即重写此文档。
