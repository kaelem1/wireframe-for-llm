# Folder: /Users/kaelem/dev/ihelp/2/src

1. **地位**：前端应用源码根目录

2. **逻辑**：`main.tsx` 挂载应用；`App.tsx` 组装编辑器、弹层与快捷键；`stores/` 管状态，`components/` 渲染工作区与预览，`utils/` 提供 Schema/AI/导入导出能力。

3. **约束**：只保留编辑器必需代码；视觉与数据结构必须对应导出 JSON。

## 成员清单

- `components/`：编辑器界面与预览组件
- `stores/`：Zustand 状态与撤销重做
- `types/`：结构化 JSON 类型
- `utils/`：常量、AI、导入导出与布局辅助
- `App.tsx`：应用壳
- `index.css`：全局样式
- `main.tsx`：挂载入口

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
