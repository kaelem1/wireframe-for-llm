# Folder: /Users/kaelem/dev/ihelp/2/src

1. **地位**：前端应用源码根目录

2. **逻辑**：`main.tsx` 挂载应用；`App.tsx` 组装单一三栏编辑器壳层、底部画板条与快捷键；`stores/` 管待放置组件连续放置、多选与画板复制，`components/` 渲染左栏项目名+组件库、顶部动作条、中间画布与右栏图层/交互，`types/` 与 `utils/` 维护组件注册表与导入导出能力。

3. **约束**：只保留编辑器必需代码；视觉与数据结构必须对应导出 JSON。

## 成员清单

- `components/`：编辑器界面、统一线框画布与预览组件
- `stores/`：Zustand 状态、撤销重做与多选命令
- `types/`：结构化 JSON 类型与组件目录分组类型
- `utils/`：常量、完整组件注册表、AI、导入导出与布局辅助
- `App.tsx`：应用壳与主工作区布局
- `index.css`：全局样式与工作区两侧栏高度、滚动分配
- `main.tsx`：挂载入口

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
