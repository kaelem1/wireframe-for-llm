# Folder: /Users/kaelem/dev/ihelp/2/src/components

1. **地位**：编辑器与预览界面组件层

2. **逻辑**：组件面板添加块；`BoardCanvas` 负责选中、拖拽缩放、fit 缩放与右键菜单；交互面板、底部画板条、预览层、设置与 AI 面板共同驱动主工作流。

3. **约束**：不引入外部 UI 库；所有块用 HTML 绝对定位渲染。

## 成员清单

- `Toolbar.tsx`：顶部工具栏
- `ComponentPalette.tsx`：左侧组件面板
- `BoardCanvas.tsx`：中央画布、选中、拖拽缩放与 fit 缩放
- `BoardCanvas.test.tsx`：画布选中回归测试
- `InteractionPanel.tsx`：右侧交互与图层面板
- `BoardStrip.tsx`：底部画板条
- `PreviewOverlay.tsx`：预览模式
- `SetupDialog.tsx`：新建项目弹层
- `SettingsDialog.tsx`：设置与左右对比的 AI 还原测试
- `AIGeneratorDialog.tsx`：AI 生成弹层
- `WireframeBlock.tsx`：通用线框块渲染

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
