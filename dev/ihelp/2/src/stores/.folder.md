# Folder: /Users/kaelem/dev/ihelp/2/src/stores

1. **地位**：应用状态与编辑命令中心

2. **逻辑**：以 Zustand 保存项目、设置、待放置组件、多选集合与预览状态；以 immer patches 支撑撤销重做，并承接连续放置、多选移动、删除、画板复制与图层重排。

3. **约束**：项目结构变化必须可导出为 JSON；历史记录仅围绕项目数据。

## 成员清单

- `appStore.ts`：核心状态仓库，处理多选、待放置创建、画板名防重、图层重排、批量移动与预览栈

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
