# Folder: /Users/kaelem/dev/ihelp/2/src/utils

1. **地位**：编辑器共用辅助能力

2. **逻辑**：集中设备/组件常量、布局与导入导出辅助、AI 请求与 iframe 预览构造。

3. **约束**：仅保留复用度明确的纯函数；不要堆叠抽象层。

## 成员清单

- `constants.ts`：设备、组件与交互常量
- `project.ts`：项目创建、组件布局、JSON 导入导出
- `storage.ts`：localStorage 快照读写与防抖保存
- `ai.ts`：OpenAI 兼容请求、提示词与 iframe 构造

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
