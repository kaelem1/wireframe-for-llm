# Folder: /Users/kaelem/dev/sumsession

1. **地位**：批量整理 Codex 会话 jsonl 的最小工具目录。
2. **逻辑**：`codex_qa_summary.py` 读取 2026/03 和 2026/04 的会话记录，只保留 user/assistant 轮次；先按 turn 分块，再只分析带错误/构建/API 信号的 chunk；单 worker 节流调用 SiliconFlow 做 map/reduce；最终汇总为 Markdown 速查手册。
3. **约束**：只处理指定输入目录，API key 只读 `OPENAI_API_KEY`，错误写入 `errors.log`，输出为 `codex_qa_summary.md`。

## 成员清单

- `codex_qa_summary.py`：单 worker Map-Reduce 主脚本。
- `codex_qa_summary.md`：生成结果。
- `errors.log`：失败记录。
## 触发器

本目录新增或调整脚本、输出格式、数据流时，立即重写此文档。
