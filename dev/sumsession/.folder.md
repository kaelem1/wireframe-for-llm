# Folder: /Users/kaelem/dev/sumsession

1. **地位**：批量整理 Codex 会话 jsonl 的最小工具目录。
2. **逻辑**：`codex_qa_summary.py` 负责批量抽取与断点状态；本次第二波整合基于分片 JSON 做全局去重，按固定 tag 顺序生成 `codex_qa_summary.md`。
3. **约束**：只处理指定输入目录与分片结果，API key 只读 `OPENAI_API_KEY`，错误写入 `errors.log`，最终摘要不保留 evidence。

## 成员清单

- `codex_qa_summary.py`：单 worker Map-Reduce 主脚本。
- `.codex_qa_state.jsonl`：运行中的断点状态。
- `codex_qa_summary.md`：生成结果。
- `errors.log`：失败记录。
## 触发器

本目录新增或调整脚本、输出格式、数据流时，立即重写此文档。
