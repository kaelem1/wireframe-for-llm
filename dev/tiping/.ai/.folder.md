# Folder: /Users/kaelem/dev/tiping/.ai

1. **地位**：项目级认知锚点，记录当前状态与长期约束

2. **逻辑**：任务开始先读本目录，再据此调整实现范围；任务结束回写状态与决策

3. **约束**：单次写入 `current.md` 或 `decisions.md` 不超过 200 字；禁止新增其他说明文档

## 成员清单

- `current.md`：当前任务状态与最新结果
- `decisions.md`：长期保留的架构与约束决策

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。

