# Folder: /Users/kaelem/dev/tiping

1. **地位**：Web 原型工具根目录，承载构建配置、总说明与顶层约束文档

2. **逻辑**：Vite 启动 `src/` 单页应用；项目状态持久化在浏览器 localStorage；根目录文档同步全局状态、构建约束与源码入口关系

3. **约束**：禁止在根目录新增临时说明文档；忽略构建副产物；目录结构变化时同步本文件与 `.ai/`

## 成员清单

- `.ai/`：全局状态与长期决策文档
- `.gitignore`：本地构建产物忽略规则
- `index.html`：SPA 挂载入口
- `package-lock.json`：依赖锁定文件
- `package.json`：依赖与脚本定义
- `postcss.config.cjs`：PostCSS 配置
- `readme.md`：项目安装与使用说明
- `src/`：应用源码
- `tailwind.config.cjs`：Tailwind 主题配置
- `tsconfig.app.json`：前端 TypeScript 配置
- `tsconfig.json`：TypeScript 引用入口
- `tsconfig.node.json`：Node 侧 TypeScript 配置
- `vite.config.ts`：Vite 配置

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
