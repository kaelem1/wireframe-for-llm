# Folder: /Users/kaelem/dev/tiping/src

1. **地位**：前端单页应用源码目录

2. **逻辑**：`main.tsx` 挂载应用，`App.tsx` 在项目列表与编辑器间切换，状态集中在 `store.ts`，AI/导出能力由同级模块提供

3. **约束**：保持纯客户端实现；新增目录或职责变化时同步本文件；避免把业务逻辑塞进样式文件

## 成员清单

- `App.tsx`：应用入口组件
- `ai.ts`：SiliconFlow OpenAI 兼容请求
- `constants.ts`：常量与预设
- `exporters.ts`：AI 导出与对齐比较
- `index.css`：全局样式与 Tailwind 层
- `main.tsx`：React 挂载入口
- `store.ts`：Zustand 全局状态
- `types.ts`：核心数据类型
- `utils.ts`：项目、元素与通用工具函数
- `components/`：页面与画板组件

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
