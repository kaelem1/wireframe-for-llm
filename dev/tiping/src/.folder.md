# Folder: /Users/kaelem/dev/tiping/src

1. **地位**：前端单页应用源码目录，承载项目列表、编辑器、预览与 AI 能力

2. **逻辑**：`main.tsx` 挂载 `App.tsx`；`App.tsx` 只负责在 `HomePage` 与 `EditorPage` 间切换；扁平文件 `store.ts`、`constants.ts`、`ai.ts`、`exporters.ts` 提供状态与能力，`components/` 承载界面

3. **约束**：保持单页 SPA；优先在现有扁平文件与 `components/` 内补功能，避免再引入第二套目录结构

## 成员清单

- `App.tsx`：应用壳与页面切换
- `ai.ts`：SiliconFlow 请求与 AI 生成/对齐
- `components/`：首页、编辑器与画板组件
- `constants.ts`：常量与预设
- `exporters.ts`：AI 导出与对齐比对
- `index.css`：Tailwind 入口与全局样式
- `main.tsx`：React 挂载入口
- `store.ts`：全局状态与编辑器动作
- `types.ts`：核心数据结构定义
- `utils.ts`：通用工具与项目/元素构造

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
