# Folder: /Users/kaelem/dev/tiping/src/components

1. **地位**：应用界面层，承载项目列表、编辑器与 Konva 画板渲染

2. **逻辑**：`HomePage.tsx` 负责项目管理入口，`EditorPage.tsx` 负责三栏工作区与弹窗，`BoardCanvas.tsx` 负责单画板渲染与元素编辑

3. **约束**：组件只消费 store 暴露的动作；跨模块通用逻辑回收到 `src/` 同级工具文件；目录变动时同步本文件

## 成员清单

- `BoardCanvas.tsx`：单个画板的 Konva 渲染与交互
- `EditorPage.tsx`：编辑器主界面
- `HomePage.tsx`：项目列表首页

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
