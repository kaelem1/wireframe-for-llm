# Folder: /Users/kaelem/dev/ihelp/2/src/stores

1. **地位**：应用状态与编辑命令中心

2. **逻辑**：以 Zustand 保存项目、设置、locale、待放置组件与多选集合；以 immer patches 支撑撤销重做，并承接连续放置、多选移动、删除、组件复制粘贴、画板复制、图层重排与图层命名防重；待放置期间，其他图层选中入口保持禁用；locale 每次初始化都取浏览器语言。

3. **约束**：项目结构变化必须可导出为 JSON；历史记录仅围绕项目数据。

## 成员清单

- `appStore.ts`：核心状态仓库，处理浏览器 locale、多选、待放置创建、画板名防重、图层重排与批量移动

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
