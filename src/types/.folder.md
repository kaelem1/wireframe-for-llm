# Folder: /Users/kaelem/dev/ihelp/2/src/types

1. **地位**：导出 JSON 与内部实体类型定义

2. **逻辑**：定义项目、画板、组件、交互、AI 设置、导出 JSON 扩展结构、单一 wireframe purpose 状态与兼容别名，供 store 与 UI 共用。

3. **约束**：字段命名必须与导出 JSON 保持一致或可直接映射。

## 成员清单

- `schema.ts`：项目、画板、组件、交互、AI 状态与导出 JSON 类型定义

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
