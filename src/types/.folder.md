# Folder: /Users/kaelem/dev/wireframe-for-llm/src/types

1. **地位**：导出 JSON 与内部实体类型定义

2. **逻辑**：定义项目、画板、七类活跃组件、旧导入组件兼容类型、交互、AI 设置、locale、导出 JSON 扩展结构、单一 wireframe purpose 状态与兼容别名，供 store 与 UI 共用；组件内部可存 description，导出组件会映射为 info 并继续支持 clipped 标记，顶层 `_instructions` 额外声明手绘容差与禁 emoji 约束；showModal 交互沿用 `target` 承载弹窗描述或历史 modal 目标。

3. **约束**：字段命名必须与导出 JSON 保持一致或可直接映射。

## 成员清单

- `schema.ts`：项目、画板、七类活跃组件、旧导入组件兼容、交互、AI 状态、locale 与导出 JSON 类型定义

## 触发器

本目录增删文件，或架构、职责、数据流变化时，立即重写此文档。
